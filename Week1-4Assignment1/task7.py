your_variable_name = \
["abcdefghijklm",
 "nopqrstuvwxyz"]


key = str(input("Enter a string to type: "))
# signal for stopping the loop should base on the count on the key
stop_signal = len(key)
operation = ""

print(stop_signal)


    
    def 
while stop_signal<=0:
    
    # Everytime it press the key, stop_signal -=1
    # and operation add a 'p'
    operation += "p"
    stop_signal -= 1 

print("The robot must perform the following operations:\n")